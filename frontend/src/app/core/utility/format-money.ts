/**
 * @deprecated Re-export shim — import from {@code ./format-locale} instead. Kept so callers
 * don't break mid-migration; will be removed in a later phase.
 */

export {formatMoney} from "./format-locale";
